@:used
private class geom.constraints._Interval.RadianInterval_Impl_ {

	@:impl @:noCompletion
	public static inline function _new() {
		var this1 = new geom.structure.Range(- Math.PI, Math.PI);
		return cast this1;
	}

	@:impl
	public static inline function wrap(this1:geom.constraints.Interval, v:Float) {
		if ((v == (if ((v <= cast this1.l)) cast this1.l else if ((v >= cast this1.u)) cast this1.u else v))) return v else return v - Math.PI * 2 * Math.floor((v + Math.PI) / (Math.PI * 2));
	}

	public static inline function inPiWrap(v:Float) {
		var this1 = new geom.structure.Range(- Math.PI, Math.PI);
		var this2 = cast this1;
		return v == (if ((v <= cast this2.l)) cast this2.l else if ((v >= cast this2.u)) cast this2.u else v);
	}

	public static inline function wrapRadian(v:Float) {
		var this1 = new geom.structure.Range(- Math.PI, Math.PI);
		var this2 = cast this1;
		if ((v == (if ((v <= cast this2.l)) cast this2.l else if ((v >= cast this2.u)) cast this2.u else v))) return v else return v - Math.PI * 2 * Math.floor((v + Math.PI) / (Math.PI * 2));
	}
}