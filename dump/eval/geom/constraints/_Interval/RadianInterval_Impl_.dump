@:used
private class geom.constraints._Interval.RadianInterval_Impl_ {

	@:impl @:noCompletion
	public static inline function _new() {
		var this = new geom.structure.Range(- Math.PI, Math.PI);
		return cast this;
	}

	@:impl
	public static inline function wrap(this:geom.constraints.Interval, v:Float) {
		if ((v == (if ((v <= cast this.l)) cast this.l else if ((v >= cast this.u)) cast this.u else v))) return v else return v - Math.PI * 2 * Math.floor((v + Math.PI) / (Math.PI * 2));
	}

	public static inline function inPiWrap(v:Float) {
		var this = new geom.structure.Range(- Math.PI, Math.PI);
		var this = cast this;
		return v == (if ((v <= cast this.l)) cast this.l else if ((v >= cast this.u)) cast this.u else v);
	}

	public static inline function wrapRadian(v:Float) {
		var this = new geom.structure.Range(- Math.PI, Math.PI);
		var this = cast this;
		if ((v == (if ((v <= cast this.l)) cast this.l else if ((v >= cast this.u)) cast this.u else v))) return v else return v - Math.PI * 2 * Math.floor((v + Math.PI) / (Math.PI * 2));
	}
}